# 마이페이지 P2 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 마이페이지에 결과 이미지 저장 · 두 결과 오버레이 레이더 비교 · 테스트별 그룹/필터를 더한다(결과 삭제 제외, DB 변경 없음).

**Architecture:** 전부 클라이언트 측. `me.js`가 `results`(RLS 소유자-읽기) 조회 rows를 `test_id`로 그룹핑·필터하고, 카드별 PNG 저장(html2canvas), 2개 선택 시 `radar.js`의 새 `renderCompareRadar`로 오버레이 비교 모달을 띄운다. 테스트 특유 로직은 `getTest(test_id)` 모듈로 위임.

**Tech Stack:** Vanilla JS(정적), `html2canvas`(vendor), Supabase(변경 없음). 검증: `node --check` + agent-browser.

**참조 스펙:** `docs/superpowers/specs/2026-09-10-mypage-p2-design.md`

---

## 파일 구조

| 파일 | 책임 | 변경 |
|---|---|---|
| `radar.js` | `renderCompareRadar(svgEl, datasets, opts)` 추가(기존 `renderRadar` 불변) | 수정 |
| `me.html` | `vendor/html2canvas.min.js` 로드 추가 | 수정 |
| `me.js` | 그룹/필터 렌더, 카드 액션(이미지 저장·비교 토글), 비교 바·모달, 이미지 저장 | 수정 |
| `styles.css` | 탭·그룹헤더·카드액션·선택강조·비교바·모달·범례(append) | 수정 |

**현재 `me.js` 구조(변경 근거):** `root=#me-root`; `toast`/`fmtDate`/`shareUrl`; `renderAuth`; `login`/`renderLogin`; `resultCard(row)`(=.share-card: 커버[검사일 kicker·emoji·title=type.title·sub=code·role]·radar svg·summary·`.sc-remove`"공유 링크 복사"); `messageCard`; `renderResults(session)`(results 조회→빈상태 name-card | `.share-grid` of resultCard); `main()`.

---

## Task 1: `radar.js` — `renderCompareRadar`

**Files:**
- Modify: `radar.js` (기존 `renderRadar` 아래에 추가)

- [ ] **Step 1: `renderCompareRadar` 추가**

기존 `renderRadar`/`radarPoint`/`RADAR_ORDER`/`FACTORS` 재사용. 여러 데이터셋을 한 오각형에 겹쳐 그린다. 축 라벨은 중립색으로 1회.

```js
// 여러 결과를 한 레이더에 겹쳐 그린다(비교용). datasets: [{ pct, color, label }, …].
// 격자·축선·축 라벨(영문)은 1회, 각 dataset은 자기 color로 반투명 폴리곤+스트로크+정점.
function renderCompareRadar(svgEl, datasets, opts = {}) {
  if (!svgEl || !datasets || !datasets.length) return;
  const cx = opts.cx ?? 230, cy = opts.cy ?? 200, R = opts.R ?? 118;
  const grid = [0.25, 0.5, 0.75, 1];
  let out = "";
  grid.forEach((g) => {
    const pts = RADAR_ORDER.map((_, i) => radarPoint(cx, cy, R, i, g).map((n) => n.toFixed(1)).join(",")).join(" ");
    out += `<polygon points="${pts}" fill="none" stroke="rgba(255,255,255,0.08)" stroke-width="1"/>`;
  });
  RADAR_ORDER.forEach((_, i) => {
    const [x, y] = radarPoint(cx, cy, R, i, 1);
    out += `<line x1="${cx}" y1="${cy}" x2="${x.toFixed(1)}" y2="${y.toFixed(1)}" stroke="rgba(255,255,255,0.08)" stroke-width="1"/>`;
  });
  datasets.forEach((ds) => {
    const color = ds.color || "#4b8ffc";
    const pts = RADAR_ORDER.map((k, i) => radarPoint(cx, cy, R, i, Math.max(ds.pct[k], 4) / 100).map((n) => n.toFixed(1)).join(",")).join(" ");
    out += `<polygon points="${pts}" fill="${color}" fill-opacity="0.18" stroke="${color}" stroke-width="2.5" stroke-linejoin="round"/>`;
    RADAR_ORDER.forEach((k, i) => {
      const [dx, dy] = radarPoint(cx, cy, R, i, Math.max(ds.pct[k], 4) / 100);
      out += `<circle cx="${dx.toFixed(1)}" cy="${dy.toFixed(1)}" r="4" fill="${color}" stroke="#0f1b34" stroke-width="1.5"/>`;
    });
  });
  RADAR_ORDER.forEach((k, i) => {
    const f = FACTORS[k];
    const [lx, ly] = radarPoint(cx, cy, R + 26, i, 1);
    const anchor = i === 0 ? "middle" : lx > cx + 4 ? "start" : lx < cx - 4 ? "end" : "middle";
    out += `<text x="${lx.toFixed(1)}" y="${(ly + 3).toFixed(1)}" text-anchor="${anchor}" class="radar-axis-label" fill="#9fb2ce">${f.english}</text>`;
  });
  svgEl.innerHTML = out;
}
```

- [ ] **Step 2: 검증**

Run: `node --check radar.js`
Expected: 통과.
Run(agent-browser, 로컬 서버): 임의 페이지에서 빈 `<svg viewBox="0 0 460 420">`에 `renderCompareRadar(svg, [{pct:{O:60,C:55,E:50,A:55,N:65},color:'#4b8ffc',label:'A'},{pct:{O:40,C:70,E:60,A:45,N:30},color:'#f5a623',label:'B'}])` 호출 → `svg`에 두 색 폴리곤(2개) + 5 축 라벨 존재.
Expected: 폴리곤 2개·축라벨 5개 렌더, 콘솔 오류 없음. 기존 `renderRadar` 결과 화면 회귀 없음(별도 확인).

- [ ] **Step 3: 커밋**

```bash
git add radar.js
git commit -m "feat(radar): renderCompareRadar 추가(여러 결과 오버레이)"
```

---

## Task 2: 테스트별 그룹/필터 + 카드 이미지 저장

**Files:**
- Modify: `me.html` (html2canvas 로드)
- Modify: `me.js` (`renderResults` 그룹/필터, `resultCard` 액션 영역 + 이미지 저장)
- Modify: `styles.css` (탭·그룹헤더·카드액션 append)

- [ ] **Step 1: `me.html` — html2canvas 로드**

`<script src="me.js">` 앞에 추가:
```html
<script src="vendor/html2canvas.min.js"></script>
```

- [ ] **Step 2: `me.js` — 카드 액션 영역 + 이미지 저장**

`resultCard(row)`의 마지막 "공유 링크 복사" 버튼을 **액션 컨테이너**로 감싸고 "이미지 저장" 버튼 추가:
```js
const actions = document.createElement("div");
actions.className = "sc-actions";
const copy = document.createElement("button");
copy.className = "sc-btn";
copy.textContent = "공유 링크 복사";
copy.addEventListener("click", () => {
  navigator.clipboard?.writeText(shareUrl(row.share_id)).then(
    () => toast("공유 링크를 복사했어요 ✓"),
    () => toast("복사에 실패했어요")
  );
});
const img = document.createElement("button");
img.className = "sc-btn";
img.textContent = "이미지 저장";
img.addEventListener("click", () => saveCardImage(card, row, img));
actions.append(copy, img);
card.append(cover, svg, summary, actions);   // 기존 append 대체
```

`saveCardImage` 추가(액션 버튼은 캡처에서 제외):
```js
async function saveCardImage(cardEl, row, btn) {
  if (typeof html2canvas === "undefined") { toast("이미지 저장을 사용할 수 없어요"); return; }
  const type = getTest(row.test_id || "ocean").buildProfile(row.scores).type;
  const original = btn?.textContent;
  if (btn) { btn.disabled = true; btn.textContent = "생성 중…"; }
  try {
    const canvas = await html2canvas(cardEl, {
      backgroundColor: "#141a2e", scale: 2, useCORS: true, logging: false,
      ignoreElements: (el) => el.classList?.contains("sc-actions"),
    });
    const d = new Date(row.created_at);
    const ymd = `${d.getFullYear()}${String(d.getMonth() + 1).padStart(2, "0")}${String(d.getDate()).padStart(2, "0")}`;
    const name = `${row.test_id || "ocean"}_${type.title.replace(/^The\s+/, "").replace(/\s+/g, "_")}_${ymd}.png`;
    canvas.toBlob((blob) => {
      if (!blob) { toast("이미지 생성에 실패했어요"); return; }
      const url = URL.createObjectURL(blob);
      const a = document.createElement("a"); a.href = url; a.download = name; a.click();
      URL.revokeObjectURL(url);
      toast("이미지를 저장했어요 ✓");
    }, "image/png");
  } catch (e) { console.error(e); toast("이미지 생성에 실패했어요"); }
  finally { if (btn) { btn.disabled = false; btn.textContent = original; } }
}
```
(`.share-card` 실제 배경색과 `#141a2e`가 다르면 `.share-card`에 맞춰 조정.)

- [ ] **Step 3: `me.js` — `renderResults` 그룹/필터**

`renderResults(session)`의 성공 분기(빈 상태 제외)를 그룹/필터 렌더로 교체:
```js
// data: [{…, test_id}] (created_at desc). test_id별 그룹 + 필터 탭.
const groups = [];                    // [{ id, name, icon, rows }]
data.forEach((row) => {
  const id = row.test_id || "ocean";
  let g = groups.find((x) => x.id === id);
  if (!g) { const m = getTest(id)?.meta; g = { id, name: m?.name || id, icon: m?.icon || "🧪", rows: [] }; groups.push(g); }
  g.rows.push(row);
});

let activeFilter = "all";
function paint() {
  const wrap = document.createElement("div");
  // 필터 탭
  const tabs = document.createElement("div"); tabs.className = "me-tabs";
  const mkTab = (id, label) => {
    const b = document.createElement("button");
    b.className = "me-tab" + (activeFilter === id ? " active" : "");
    b.textContent = label;
    b.addEventListener("click", () => { activeFilter = id; render.paint(); });
    return b;
  };
  tabs.append(mkTab("all", "전체"), ...groups.map((g) => mkTab(g.id, g.name)));
  wrap.appendChild(tabs);
  // 그룹
  groups.filter((g) => activeFilter === "all" || g.id === activeFilter).forEach((g) => {
    const head = document.createElement("div"); head.className = "me-group-head";
    head.textContent = `${g.icon} ${g.name} · ${g.rows.length}`;
    const grid = document.createElement("div"); grid.className = "share-grid";
    g.rows.forEach((row) => grid.appendChild(resultCard(row)));
    wrap.append(head, grid);
  });
  root.replaceChildren(wrap);
}
const render = { paint };   // 재호출용
paint();
```
(구현 시 `activeFilter`/`paint`를 renderResults 스코프 안에 두어 탭 클릭이 재렌더하도록 한다. 위 `render.paint()`는 그 참조. 단순화해도 됨 — 요지는 탭 클릭→필터 반영 재렌더.)

- [ ] **Step 4: `styles.css` — 탭·그룹헤더·카드액션 append**

```css
/* ===== 마이페이지 P2: 탭·그룹·카드 액션 ===== */
.me-tabs { display: flex; flex-wrap: wrap; gap: 8px; margin: 4px 0 20px; }
.me-tab { font-family: inherit; font-weight: 700; font-size: .9rem; padding: 8px 16px; border-radius: 999px; border: 1px solid var(--line); background: var(--card); color: var(--muted); cursor: pointer; }
.me-tab.active { background: var(--primary); color: #fff; border-color: var(--primary); }
.me-group-head { font-weight: 800; font-size: 1.05rem; color: var(--text); margin: 22px 0 12px; }
.sc-actions { display: flex; flex-wrap: wrap; gap: 8px; margin-top: 4px; }
.sc-btn { font-family: inherit; font-weight: 600; font-size: .82rem; padding: 7px 12px; border-radius: 999px; border: 1px solid var(--r-line); background: transparent; color: var(--r-text); cursor: pointer; }
.sc-btn:hover { border-color: var(--r-accent); color: #fff; }
.sc-btn:disabled { opacity: .6; cursor: default; }
```
(`.sc-btn`는 다크 카드 안이므로 `.share-card` 스코프의 `--r-*` 토큰을 쓴다. 위치가 카드 밖이면 라이트 토큰으로.)

- [ ] **Step 5: 검증**

Run: `node --check me.js`
Expected: 통과. `grep -n "sc-actions\|saveCardImage\|me-tabs" me.js` 존재.
(로그인 필요한 실렌더는 Task 4에서 수동 확인. 구조/구문만 여기서.)

- [ ] **Step 6: 커밋**

```bash
git add me.html me.js styles.css
git commit -m "feat(mypage): 테스트별 그룹/필터 + 카드 이미지 저장"
```

---

## Task 3: 두 결과 비교 (오버레이 레이더 모달)

**Files:**
- Modify: `me.js` (비교 토글·선택 상태·비교 바·모달·가드)
- Modify: `styles.css` (선택강조·비교바·모달·범례 append)

- [ ] **Step 1: `me.js` — 비교 상태 + 카드 토글**

파일 상단(스코프)에 선택 상태:
```js
const compareSel = new Map();   // rowId → row (최대 2)
```
`resultCard(row)` 액션에 "비교" 토글 추가(Task 2의 `actions`에 append):
```js
const cmp = document.createElement("button");
cmp.className = "sc-btn sc-compare";
cmp.textContent = "비교";
cmp.setAttribute("aria-pressed", "false");
cmp.addEventListener("click", () => toggleCompare(row, card, cmp));
actions.append(cmp);   // copy, img 다음
```
```js
function toggleCompare(row, cardEl, btn) {
  const key = row.id;
  if (compareSel.has(key)) { compareSel.delete(key); cardEl.classList.remove("is-selected"); btn.setAttribute("aria-pressed", "false"); }
  else {
    if (compareSel.size >= 2) { toast("두 개까지 선택할 수 있어요"); return; }
    compareSel.set(key, row); cardEl.classList.add("is-selected"); btn.setAttribute("aria-pressed", "true");
  }
  renderCompareBar();
}
```

- [ ] **Step 2: `me.js` — 비교 바**

```js
function renderCompareBar() {
  let bar = document.querySelector(".compare-bar");
  if (compareSel.size < 2) { bar?.remove(); return; }
  if (!bar) {
    bar = document.createElement("div"); bar.className = "compare-bar"; document.body.appendChild(bar);
  }
  bar.innerHTML = "";
  const label = document.createElement("span"); label.textContent = "2개 선택됨";
  const go = document.createElement("button"); go.className = "btn btn-primary"; go.textContent = "비교하기";
  go.addEventListener("click", openCompare);
  const clear = document.createElement("button"); clear.className = "btn btn-ghost"; clear.textContent = "선택 해제";
  clear.addEventListener("click", clearCompare);
  bar.append(label, go, clear);
}
function clearCompare() {
  compareSel.clear();
  document.querySelectorAll(".share-card.is-selected").forEach((c) => c.classList.remove("is-selected"));
  document.querySelectorAll(".sc-compare").forEach((b) => b.setAttribute("aria-pressed", "false"));
  renderCompareBar();
}
```

- [ ] **Step 3: `me.js` — 비교 모달(가드 + 오버레이 레이더 + 이미지 저장)**

```js
const CMP_COLORS = ["#4b8ffc", "#f5a623"];
function openCompare() {
  const rows = [...compareSel.values()];
  if (rows.length !== 2) return;
  if ((rows[0].test_id || "ocean") !== (rows[1].test_id || "ocean")) { toast("같은 테스트끼리만 비교할 수 있어요"); return; }
  const items = rows.map((row, i) => {
    const profile = getTest(row.test_id || "ocean").buildProfile(row.scores);
    return { row, profile, color: CMP_COLORS[i] };
  });

  const backdrop = document.createElement("div"); backdrop.className = "compare-backdrop";
  const modal = document.createElement("div"); modal.className = "compare-modal";

  const heads = document.createElement("div"); heads.className = "cmp-heads";
  items.forEach((it) => {
    const h = document.createElement("div"); h.className = "cmp-head";
    const dot = document.createElement("span"); dot.className = "cmp-dot"; dot.style.background = it.color;
    const t = document.createElement("div");
    const title = document.createElement("div"); title.className = "cmp-title"; title.textContent = `${it.profile.type.emoji || ""} ${it.profile.type.title}`;
    const date = document.createElement("div"); date.className = "cmp-date"; date.textContent = fmtDate(it.row.created_at);
    t.append(title, date); h.append(dot, t); heads.appendChild(h);
  });

  const svg = document.createElementNS("http://www.w3.org/2000/svg", "svg");
  svg.setAttribute("class", "cmp-radar"); svg.setAttribute("viewBox", "0 0 460 420"); svg.setAttribute("role", "img"); svg.setAttribute("aria-label", "비교 레이더 차트");

  const legend = document.createElement("div"); legend.className = "cmp-legend";
  items.forEach((it) => {
    const l = document.createElement("span"); l.className = "cmp-leg";
    const d = document.createElement("i"); d.style.background = it.color;
    l.append(d, document.createTextNode(fmtDate(it.row.created_at)));
    legend.appendChild(l);
  });

  const acts = document.createElement("div"); acts.className = "sc-actions";
  const saveBtn = document.createElement("button"); saveBtn.className = "sc-btn"; saveBtn.textContent = "이미지 저장";
  const closeBtn = document.createElement("button"); closeBtn.className = "sc-btn"; closeBtn.textContent = "닫기";
  closeBtn.addEventListener("click", () => backdrop.remove());
  backdrop.addEventListener("click", (e) => { if (e.target === backdrop) backdrop.remove(); });
  acts.append(saveBtn, closeBtn);

  modal.append(heads, svg, legend, acts);
  backdrop.appendChild(modal); document.body.appendChild(backdrop);
  renderCompareRadar(svg, items.map((it) => ({ pct: it.row.scores, color: it.color })));

  saveBtn.addEventListener("click", async () => {
    if (typeof html2canvas === "undefined") { toast("이미지 저장을 사용할 수 없어요"); return; }
    const canvas = await html2canvas(modal, { backgroundColor: "#141a2e", scale: 2, useCORS: true,
      ignoreElements: (el) => el.classList?.contains("sc-actions") });
    canvas.toBlob((blob) => {
      if (!blob) { toast("이미지 생성에 실패했어요"); return; }
      const url = URL.createObjectURL(blob); const a = document.createElement("a");
      a.href = url; a.download = "compare.png"; a.click(); URL.revokeObjectURL(url);
      toast("이미지를 저장했어요 ✓");
    }, "image/png");
  });
}
```

- [ ] **Step 4: `styles.css` — 선택강조·비교바·모달·범례 append**

```css
/* 비교 선택/바/모달 */
.share-card.is-selected { outline: 2px solid var(--primary); outline-offset: 2px; }
.sc-compare[aria-pressed="true"] { background: var(--r-accent); color: #fff; border-color: var(--r-accent); }
.compare-bar { position: fixed; left: 50%; bottom: 20px; transform: translateX(-50%); display: flex; align-items: center; gap: 12px; background: #fff; border: 1px solid var(--line); box-shadow: var(--shadow); border-radius: 999px; padding: 10px 16px; z-index: 40; }
.compare-backdrop { position: fixed; inset: 0; background: rgba(0,0,0,.5); display: flex; align-items: center; justify-content: center; z-index: 50; padding: 20px; }
.compare-modal { background: linear-gradient(160deg, #0f1b34, #14264a); color: #eaf1fb; border-radius: 20px; padding: 24px; width: 100%; max-width: 440px; }
.cmp-heads { display: flex; justify-content: space-between; gap: 12px; }
.cmp-head { display: flex; gap: 8px; align-items: flex-start; }
.cmp-dot { width: 12px; height: 12px; border-radius: 50%; margin-top: 4px; flex: 0 0 auto; }
.cmp-title { font-weight: 800; }
.cmp-date { font-size: .8rem; color: #9fb2ce; }
.cmp-radar { width: 100%; max-width: 340px; height: auto; display: block; margin: 8px auto; }
.cmp-legend { display: flex; justify-content: center; gap: 16px; margin-bottom: 12px; font-size: .85rem; color: #cfe0f5; }
.cmp-leg { display: inline-flex; align-items: center; gap: 6px; }
.cmp-leg i { width: 12px; height: 12px; border-radius: 3px; display: inline-block; }
```

- [ ] **Step 5: 검증**

Run: `node --check me.js`
Expected: 통과. `grep -n "openCompare\|renderCompareBar\|toggleCompare\|renderCompareRadar" me.js` 존재.

- [ ] **Step 6: 커밋**

```bash
git add me.js styles.css
git commit -m "feat(mypage): 두 결과 오버레이 레이더 비교(선택·바·모달·가드)"
```

---

## Task 4: E2E 검증 + 배포

- [ ] **Step 1: `renderCompareRadar` 단독 렌더 확인(agent-browser)**

로컬 서버(`node server.js`) → 임의 로드된 페이지(예: `me.html`)에서 콘솔-eval로 빈 svg 만들어 `renderCompareRadar` 호출 → 폴리곤 2·축라벨 5 확인(로그인 불필요, 함수는 전역).

- [ ] **Step 2: 로그인 흐름 수동 확인(대화형)**

`me.html` 로그인 → 결과 2개 이상 있는 계정에서: 필터 탭(전체/오션) 동작, 카드 "이미지 저장" PNG 다운로드, "비교" 2개 선택→바→"비교하기"→오버레이 레이더 모달(두 색 폴리곤+범례)·패널 이미지 저장, 다른 test_id 비교 시 안내 토스트. (헤드리스 불가 항목 — 사용자/수동 확인 플래그.)

- [ ] **Step 3: 회귀 확인**

결과 화면·공유·대시보드의 기존 `renderRadar`가 그대로 동작(레이더 정상). `node --check radar.js me.js`.

- [ ] **Step 4: PR + 배포**

```bash
git push -u origin feat/mypage-p2
gh pr create --base main --title "feat: 마이페이지 P2(이미지 저장·비교·그룹/필터)" --body "스펙: docs/.../2026-09-10-mypage-p2-design.md"
```
머지 → GitHub Pages 반영(me.html·radar.js 200) 확인.

---

## Self-Review

**1. Spec 커버리지** — 그룹/필터(§2)→T2. 이미지 저장(§3)→T2. 비교 오버레이+가드+패널저장(§4)→T3. renderCompareRadar(§5)→T1. 스타일(§6)→T2·T3. 회귀·수용기준→T4. **누락 없음.**

**2. Placeholder 스캔** — 코드 스텝 모두 실제 코드. `paint`/`activeFilter` 재렌더는 "탭 클릭→필터 재렌더" 요지로 구현 자유. TBD/TODO 없음.

**3. 식별자 일관성** — `renderCompareRadar`(radar.js 정의 ↔ me.js openCompare 호출), `saveCardImage`/`toggleCompare`/`renderCompareBar`/`openCompare`/`compareSel`/`CMP_COLORS` 정의·사용 일치. `.sc-actions`/`.sc-btn`/`.sc-compare`/`.is-selected`/`.compare-bar`/`.compare-modal`/`.cmp-*` 마크업↔CSS 일치. `getTest(row.test_id||"ocean").buildProfile` 패턴 일관.

**리스크:** 다크 카드 실제 배경색과 캡처 `backgroundColor`(#141a2e) 불일치 시 이미지 여백색 어긋남 → 구현 시 `.share-card` 실제 배경에 맞춰 조정.
